---
import { nav, site } from '@/config/site';
import ThemeToggle from './ThemeToggle.astro';

const path = Astro.url.pathname;

/** `/` only matches itself; every other entry matches its whole subtree. */
const isCurrent = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
---

<header>
  <div class="shell bar">
    <a href="/" class="brand" aria-label={`${site.name} — home`}>
      <span class="prompt">~/</span><span class="who">{site.handle}</span>
    </a>

    <nav aria-label="Main">
      <ul>
        {
          nav.map(({ href, label }) => (
            <li>
              <a href={href} aria-current={isCurrent(href) ? 'page' : undefined}>
                {label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <ThemeToggle />
  </div>
</header>

<style>
  header {
    border-block-end: 1px solid var(--rule);
    background: color-mix(in oklab, var(--bg) 82%, transparent);
    backdrop-filter: blur(8px);
    position: sticky;
    top: 0;
    z-index: 20;
  }

  .bar {
    display: flex;
    align-items: center;
    gap: 1rem;
    min-height: 3.25rem;
    flex-wrap: wrap;
  }

  .brand {
    color: var(--fg);
    text-decoration: none;
    font-weight: 500;
    margin-inline-end: auto;
  }

  .prompt {
    color: var(--accent);
  }

  .who {
    border-block-end: 1px solid transparent;
  }

  .brand:hover .who {
    border-block-end-color: var(--accent);
  }

  ul {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem 1rem;
    padding: 0;
    margin: 0;
    font-size: var(--step--1);
  }

  a {
    color: var(--fg-dim);
    text-decoration: none;
  }

  /* Padding, not margin: this is the tap target, and 17px of text alone is far
     under the 24px WCAG floor, never mind a comfortable thumb. */
  nav a {
    display: inline-flex;
    align-items: center;
    min-height: 2.75rem;
    padding-inline: 0.15rem;
  }

  nav a::before {
    content: '[';
    color: var(--fg-faint);
  }

  nav a::after {
    content: ']';
    color: var(--fg-faint);
  }

  nav a:hover,
  nav a[aria-current='page'] {
    color: var(--accent);
  }

  nav a[aria-current='page']::before,
  nav a[aria-current='page']::after {
    color: var(--accent);
  }

  @media (max-width: 640px) {
    .bar {
      padding-block: 0.4rem 0.2rem;
      min-height: 0;
    }

    nav {
      order: 3;
      width: 100%;
    }

    nav ul {
      gap: 0 0.9rem;
    }

    nav a {
      min-height: 2.5rem;
    }

    /*
     * Not sticky on a phone. Seven links with real tap targets wrap to two rows,
     * and pinning ~120px of chrome to the top costs a sixth of the viewport on
     * every scroll. The links stay visible at the top of the page instead of
     * being hidden behind a menu button.
     */
    header {
      position: static;
      backdrop-filter: none;
    }
  }
</style>
